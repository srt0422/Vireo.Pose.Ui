import React = require("react");
import {ISocialAuthenticationProps} from "../Interfaces/ISocialAuthenticationProps";

export default class TwitterAuthentication extends React.Component<ISocialAuthenticationProps, any> {
    render(): JSX.Element;
    onClick(el: any): void;
}
